# GitHub Personal Access Token (Fine-grained, Public Repositories read-only で十分)
# サーバー側 (Route Handler) からのみ参照される。NEXT_PUBLIC_ プレフィックスは使わない。
# 未設定でも動作するが GitHub のレート制限は 10 req/min と厳しい。
GITHUB_TOKEN=
